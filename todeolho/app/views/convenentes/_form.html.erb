<%= form_for(@convenente) do |f| %>
  <% if @convenente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@convenente.errors.count, "error") %> prohibited this convenente from being saved:</h2>

      <ul>
      <% @convenente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :codigo %><br>
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :references %><br>
    <%= f.text_field :references %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
